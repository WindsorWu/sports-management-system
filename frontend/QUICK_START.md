# 快速启动指南

## 前提条件

✅ Node.js >= 16.0.0
✅ npm >= 8.0.0
✅ 后端API服务运行在 http://localhost:8000

## 快速启动

### 1. 进入项目目录
```bash
cd "/e/Python Project/sports-management-system/frontend"
```

### 2. 安装依赖 (如果还没安装)
```bash
npm install
```

### 3. 启动开发服务器
```bash
npm run dev
```

### 4. 访问应用
打开浏览器访问: **http://localhost:5173**

## 可用的命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

## 默认账号 (需在后端创建)

- **管理员账号**: admin / 密码由后端设置
- **普通用户**: 通过注册页面注册

## 访问路径

### 前台
- 首页: http://localhost:5173/
- 赛事列表: http://localhost:5173/events
- 公告列表: http://localhost:5173/announcements
- 登录: http://localhost:5173/login
- 注册: http://localhost:5173/register

### 后台 (需管理员权限)
- 管理后台: http://localhost:5173/admin
- 用户管理: http://localhost:5173/admin/users
- 赛事管理: http://localhost:5173/admin/events
- 报名管理: http://localhost:5173/admin/registrations
- 成绩管理: http://localhost:5173/admin/results

## 常见问题

### Q: 启动时提示端口被占用？
A: 修改 `vite.config.js` 中的 `server.port` 配置

### Q: API请求失败？
A: 检查后端服务是否启动，确认API地址配置正确

### Q: 登录后无法访问后台？
A: 确认当前用户具有管理员权限 (role: 'admin')

### Q: npm install 失败？
A: 尝试清除缓存: `npm cache clean --force` 然后重新安装

## 开发建议

1. 安装 **Vue DevTools** 浏览器插件
2. 使用 **VS Code** + **Volar** 插件
3. 启用 **ESLint** 和 **Prettier**
4. 查看 `README.md` 了解详细文档

## 项目结构快速浏览

```
src/
├── api/          # API接口 - 已完成
├── router/       # 路由配置 - 已完成
├── store/        # 状态管理 - 已完成
├── utils/        # 工具函数 - 已完成
├── layouts/      # 布局组件 - 已完成
└── views/        # 页面组件 - 部分完成
    ├── Login.vue     ✅ 完整功能
    ├── Register.vue  ✅ 完整功能
    ├── frontend/     ⚠️ 待开发
    └── admin/        ⚠️ 待开发
```

## 技术支持

- 查看 `README.md` - 完整文档
- 查看 `PROJECT_SUMMARY.md` - 项目总结
- 查看代码注释 - 详细说明

---

开始愉快的开发吧！ 🚀
